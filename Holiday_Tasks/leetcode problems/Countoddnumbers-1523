class Solution {
    public int countOdds(int low, int high) {
        int  c = 0;
        if(low%2==0) low = low+1;
        
        while(low<=high){
           c ++;
           low+=2;
        }
        return c;
    }
}